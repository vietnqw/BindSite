# BindSite Pipeline Architecture (Refined)

A precise technical overview of the BindSite pipeline, detailing data flow from raw sequence to binding site prediction.

```mermaid
graph TD
    %% Global Styles
    classDef storage fill:#f8f9fa,stroke:#333,stroke-width:2px,stroke-dasharray: 5 5;
    classDef cli fill:#fff3e0,stroke:#e65100,stroke-width:2px;
    classDef model fill:#e8eaf6,stroke:#3f51b5,stroke-width:2px;
    classDef data fill:#e1f5fe,stroke:#01579b,stroke-width:1px;
    classDef logic fill:#f1f8e9,stroke:#33691e,stroke-width:2px;

    %% STAGE 1: Structure Prediction
    subgraph S1 ["Stage 1: 3D Structure Prediction"]
        F1[".fa FASTA"]:::data --> C1["bindsite fold"]:::cli
        C1 --> M1["ESMFold v1"]:::model
        M1 --> P1[".pdb PDB Files"]:::storage
    end

    %% STAGE 2: Feature Extraction and Scaling
    subgraph S2 ["Stage 2: Feature Extraction and Scaling"]
        F1 --> C2["bindsite features extract"]:::cli
        P1 --> C2

        C2 --> E1["ProtT5-XL-U50<br/>→ 1024D embeddings"]:::model
        C2 --> E2["DSSP Parser<br/>→ 14D structural features"]:::logic

        E1 --> N1["Compute Min/Max Stats<br/>(training set only)"]:::logic
        N1 --> W1["Min_ProtTrans_repr.npy<br/>Max_ProtTrans_repr.npy"]:::storage
        E1 --> N2["Min-Max Normalize"]:::logic
        W1 -.->|load & apply| N2

        N2 --> J1["Concat: ProtT5 + DSSP<br/>→ 1038D Node Features"]:::data
        E2 --> J1
        J1 --> F2[".npy Feature Files"]:::storage
    end

    %% STAGE 3: Training with 5-Fold CV
    subgraph S3 ["Stage 3: Training with 5-Fold CV"]
        F2 --> DS["DeepProSiteDataset"]:::logic
        P1 --> DS
        L1[".csv Labels"]:::data --> DS

        subgraph MA ["Model Architecture"]
            DS -->|"residue features"| NP["Node Embedding<br/>1038D → 64D"]:::model
            DS -->|"3D coordinates"| EF["Edge Construction<br/>k-NN (k=30) · geometric encoding"]:::model

            NP --> ATTN
            EF -->|"fixed across layers"| ATTN

            subgraph GTL ["× 4 Graph Transformer Layer"]
                ATTN["Neighbor Attention<br/>(4 heads)"]:::model --> AN1["Add & Norm"]:::logic
                AN1 --> FFN["Feed-Forward Network"]:::model
                FFN --> AN2["Add & Norm"]:::logic
            end

            AN2 -.->|"next layer"| ATTN
            AN2 --> MLP["MLP Classifier<br/>64D → binding logit"]:::model
        end

        MLP --> W2["fold_0.pt … fold_4.pt"]:::storage
    end

    %% STAGE 4: Inference and Evaluation
    subgraph S4 ["Stage 4: Inference and Evaluation"]
        C3["bindsite evaluate"]:::cli --> EN
        C4["bindsite predict"]:::cli --> EN
        F2 --> EN
        P1 --> EN
        W2 --> EN["Ensemble Inference<br/>average over 5 fold-models"]:::logic

        EN --> TH["Apply Decision Threshold<br/>(fixed · val-optimal · max-mcc)"]:::logic

        TH -->|evaluate mode| MET["Metrics Report<br/>AUC · AUPRC · MCC · F1"]:::data
        TH -->|predict mode| OUT["Per-residue Binding<br/>Site Predictions"]:::data
    end
```

## Precision Highlights

### Feature Vector Composition (1038D)
| Source | Dim | Description |
| :--- | :--- | :--- |
| **ProtT5** | 1024 | Transformer-based sequence embeddings, Min-Max normalized. |
| **Dihedral** | 4 | Sine and Cosine of $\phi$ and $\psi$ angles. |
| **Solvent** | 1 | Relative Solvent Accessibility (RSA). |
| **Secondary** | 9 | One-hot encoded SS types (H, B, E, G, I, T, S, -, Unknown). |

### Graph Logic
- **Nodes**: $C\alpha$ atoms representing residues.
- **Edges**: Formed between each node and its **30 nearest spatial neighbors**.
- **Edge Features (16D)**: Encodes relative distances, directions, and orientations between neighboring residues using the local coordinate frames.
- **Augmentation**: During training, random noise (Gaussian) is added to node coordinates to improve model generalization.

### Training Strategy
- **5-Fold Cross-Validation**: The training set is split into 5 folds. Five independent models are trained and saved.
- **Ensemble Inference**: Predictions are generated by averaging the sigmoid outputs of all 5 fold-models, significantly reducing variance.
- **Thresholding**: Unlike standard 0.5 thresholding, the pipeline optimizes the MCC on the validation set to determine the "Val-Optimal" threshold for classification.
